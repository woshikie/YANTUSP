image: node:current-alpine

pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
    - rm -rf public
    - cp -r dist/* public

  artifacts:
    expire_in: 1 day
    paths:
      - public

  only:
    - master
